# ACL.md — ตารางสิทธิ์การเข้าถึงตามบทบาท (LeaveEasy)

> สถานะสัปดาห์นี้ (สัปดาห์ที่ 7): มีแค่ **security rule ขั้นต่ำ "ต้องล็อกอินก่อน"** เท่านั้น (ดู [firestore.rules](firestore.rules))
> ตารางด้านล่างคือ **สิทธิ์ตามที่ระบบควรเป็น** — ช่องที่มีเครื่องหมาย ⏳ คือสิ่งที่ยังไม่บังคับจริงในโค้ดวันนี้ รอ Security Rules รายห้องในสัปดาห์ที่ 8 (ดูหมายเหตุท้ายตาราง)

| บทบาท (role) | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **employee** (ผู้ขอลา) | ยื่นใบลาใหม่ (บันทึก `requesterId` เป็น uid ตัวเอง) · ดูรายการใบลา · ดูรายละเอียดใบลา · เขียนความเห็น · ลบใบของตัวเองที่ยังอยู่สถานะ "รอพิจารณา" | **อนุมัติ/ไม่อนุมัติใบลาของตัวเองหรือของใครก็ตาม** (ไม่มีปุ่มนี้เลยไม่ว่าใบใคร) · ลบใบลาของคนอื่น · แก้ไข/เพิ่ม/ลบ ประเภทการลา · เปิดดูใบลาของพนักงานคนอื่น ⏳ |
| ✅ **manager** (ผู้อนุมัติ) | ทุกอย่างที่ employee ทำได้ (ลบได้เฉพาะใบตัวเอง) · เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ) ของ **คนอื่น** · เขียนความเห็นในทุกใบ | **อนุมัติ/ไม่อนุมัติใบลาของตัวเอง** (self-approval — ผู้อนุมัติห้ามอนุมัติใบตัวเอง) · ลบใบลาของคนอื่น · แก้ไข/เพิ่ม/ลบ ประเภทการลา |
| 🧾 **hr** (ฝ่ายบุคคล) | ทุกอย่างที่ manager ทำได้ (**ยกเว้นอนุมัติใบตัวเอง — self-approval ห้ามเหมือนกัน**) · ลบใบลาของใครก็ได้ · เพิ่ม/แก้/ลบ ประเภทการลา | **เปลี่ยนสถานะใบลาที่พิจารณาแล้ว (อนุมัติ/ไม่อนุมัติ) กลับเป็น "รอพิจารณา"** — สถานะปลายทางเปลี่ยนต่อไม่ได้ ใช้กับทุก role รวม hr ด้วย (ดู leaveeasy-spec.md หัวข้อ 6) |

## ⏳ = ยังไม่บังคับจริงในโค้ดวันนี้ (รอสัปดาห์ที่ 8)

- **employee เปิดดูใบลาของพนักงานคนอื่นไม่ได้** — วันนี้ทุกคนที่ล็อกอินแล้วยังเห็นใบลาทุกใบในรายการ (หน้ารายการยังไม่กรองตาม `requesterId`)

ข้อนี้เป็นแค่เรื่องหน้ารายการยังไม่กรองข้อมูล ไม่ใช่ Security Rules จริง — คนที่รู้วิธียิง request ตรงไปที่ Firestore ยังข้ามข้อจำกัดนี้ได้อยู่ในตอนนี้ เพราะกฎ [firestore.rules](firestore.rules) วันนี้เช็คแค่ "ล็อกอินหรือยัง" เท่านั้น

## บังคับจริงแล้ววันนี้ (ระดับ UI)

- ปุ่ม **"อนุมัติ"** / **"ไม่อนุมัติ"** แสดงเฉพาะ `role` เป็น `manager` หรือ `hr` **และ** ต้องไม่ใช่ผู้ยื่นใบนั้นเอง (กัน self-approval) — `employee` มองไม่เห็นปุ่มนี้เลยไม่ว่าใบใคร
- ปุ่ม **"ลบใบลานี้"** แสดงเฉพาะเจ้าของใบ (`requesterId` ตรงกับ uid ตัวเอง) หรือ `role` เป็น `hr`
- ทุกหน้าจอต้องล็อกอินก่อนถึงจะเข้าได้ (auth guard, ดู [js/auth-guard.js](js/auth-guard.js))

(ดูโค้ดจริงที่ [js/leave-request-detail.js](js/leave-request-detail.js))
